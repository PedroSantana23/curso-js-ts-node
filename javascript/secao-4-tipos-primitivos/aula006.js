console.log([1, 2, 3, 4, "teste", true]);
console.log([1, 2, 3, 4, "teste", true].length);

["Pedro", "Aurisan"].forEach((res, index) => {
    console.log(res, index);
})

// OBS: O primeiro elemento de um Array vai ter sempre o índex 0

